version: '3.8'
services:
  web:
    build: .
    ports:
      - "5001:5000"
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=super-secure-prod-key
      - JWT_SECRET_KEY=super-secure-jwt-key
    restart: always